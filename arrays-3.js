
const arr = ['z', 'e', 'l', 'i', 'm', 'z', 'e', 'l', 'i', 'm',];
for (let i = 0; i < arr.length; i++) {
    console.log(arr[i]);
}


const are = [-3, -2, -1, 0, 1, 2, 3, 4, 5, 6];

for (let i = 0; i < are.length; i++) 
    if (are[i] > 0)
console.log(are[i]) ;